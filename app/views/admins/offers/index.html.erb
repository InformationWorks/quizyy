<% content_for :admin_content do %>

<!-- Data block -->
<article class="data-block">
    <div class="data-container">
        <header>
            <ul class="data-header-actions">
                <%= link_to(new_admins_offer_code_path,:class => "btn btn-primary") do %>
                 New Offer Code
                <% end %>   
            </ul>
        </header>
        <section class="tab-content">
        
            <!-- Tab #static -->
            <div class="tab-pane active" id="static">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Desc</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        
                        <% @offer_codes.each do |offer_code| %>
                          <tr>
                            <td><%= offer_code.code %></td>
                            <td><%= offer_code.desc %></td>
                            <td class="toolbar">
                              <div class="btn-group">
                                <%= link_to 'Show', admins_offer_code_path(offer_code)%>
                                <%= link_to 'Edit', edit_admins_offer_code_path(offer_code)%>
                                <%= link_to(admins_offer_code_path(offer_code), method: :delete, data: { confirm: 'Are you sure you want to delete this offer code?' }, :class => "btn") do %>
                                  Delete
                                <% end %>
                              </div>
                            </td>
                          </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
        </section>
    </div>
</article>
<!-- /Data block -->

<!-- Data block -->
<article class="data-block">
    <div class="data-container">
        <header>
            <h2>Offers</h2>
            <ul class="data-header-actions">
                <%= link_to(new_admins_offer_path,:class => "btn btn-primary") do %>
                 New Offer
                <% end %>   
            </ul>
        </header>
        <section class="tab-content">
        
            <!-- Tab #static -->
            <div class="tab-pane active" id="static">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Title</th>
                            <th>Desc</th>
                            <th>Start</th>
                            <th>Stop</th>
                            <th>Global</th>
                            <th>Active</th>
                            <th>Credits</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        
                        <% @offers.each do |offer| %>
                          <tr>
                            <td><%= offer.offer_code.code %></td>
                            <td><%= offer.title %></td>
                            <td><%= offer.desc %></td>
                            <td><%= offer.start %></td>
                            <td><%= offer.stop %></td>
                            <td><%= offer.global %></td>
                            <td><%= offer.active %></td>
                            <td><%= offer.credits %></td>
                            <td>
                                <%= link_to 'Show',admins_offer_path(offer)%>
                                <%= link_to 'Edit', edit_admins_offer_path(offer)%>
                                <%= link_to(admins_offer_path(offer), method: :delete, data: { confirm: 'Are you sure you want to delete this offer?' }, :class => "btn") do %>
                                  Delete
                                <% end %>
                            </td>
                          </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
        </section>
    </div>
</article>
<!-- /Data block -->

<br />

<% end %>

<%= render 'admins/admin_base' %>