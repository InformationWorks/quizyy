<%= form_for(@section_type) do |f| %>
  <% if @section_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section_type.errors.count, "error") %> prohibited this section_type from being saved:</h2>

      <ul>
      <% @section_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :instruction %><br />
    <%= f.text_area :instruction %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
