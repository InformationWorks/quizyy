<% content_for :admin_content do %>

<!-- Data block -->
<article class="data-block">
	<div class="data-container">
		<header>
			<h2>SectionTypes</h2>
			<ul class="data-header-actions">
				<%= link_to(new_section_type_path,:class => "btn btn-primary") do %>
				 Create New Section Type
				<% end %>	
			</ul>
		</header>
		<section class="tab-content">
		
			<!-- Tab #static -->
			<div class="tab-pane active" id="static">
				<table class="table table-hover">
					<thead>
						<tr>
							<th>Name</th>
							<th>Instruction</th>
							<th></th>
						</tr>
					</thead>
					<tbody>
						
						<% @section_types.each do |section_type| %>
						  <tr>
						    <td><%= section_type.name %></td>
						    <td><%= section_type.instruction %></td>
						    <td class="toolbar">
						      <div class="btn-group">
						    	<%= link_to(section_type, :class => "btn") do %>
						    	  <span class="awe-eye-open"></span>
						    	<% end %>
						    	<%= link_to(edit_section_type_path(section_type), :class => "btn") do %>
						    	  <span class="awe-wrench"></span>
						    	<% end %>
						    	<%= link_to(section_type, method: :delete, data: { confirm: 'Are you sure you want to delete this package?' }, :class => "btn") do %>
						    	  <span class="awe-remove"></span>
						    	<% end %>
						      </div>
						    </td>
						  </tr>
						<% end %>
					</tbody>
				</table>
		</section>
		<footer class="error">
			<div class="alert alert-error">
				<strong style="font-size: 15px;">DO NOT DELETE OR RENAME SECTION TYPE IF YOU DON'T KNOW WHAT YOU ARE DOING !</strong>
			</div>
		</footer>
	</div>
</article>
<!-- /Data block -->

<br />

<% end %>

<%= render 'admins/admin_base' %>
